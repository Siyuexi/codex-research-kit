---
description: Compatibility alias for /codex-memory.
---

# /memory-manager

Compatibility alias for the old Claude kit command name.

Use the `codex-memory` skill and follow `/codex-memory` semantics. This command never auto-summarizes sessions and never calls a model or sub-agent from a hook.
