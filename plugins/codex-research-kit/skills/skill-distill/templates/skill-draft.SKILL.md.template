---
name: {{SLUG}}
description: "[DRAFT — generated by distill on {{DATE}}, not yet approved by user] {{DESCRIPTION}}"
version: 0.0.1
author: distill
license: MIT
tags: [Draft, Distill-Generated]
status: draft
disabled: true
distill_source:
  run_date: {{DATE}}
  frequency: {{FREQUENCY}}
  signal_weight: {{SIGNAL_WEIGHT}}
  score: {{SCORE}}
  evidence_sessions: {{EVIDENCE_SESSIONS_YAML}}
---

# {{TITLE}} — DRAFT

> ⚠️ This skill was auto-generated by the `distill` skill on {{DATE}}.
> It is **disabled by default** (`disabled: true` in frontmatter). To enable it:
> 1. Read this file end-to-end and edit anything wrong.
> 2. Remove `status: draft` and `disabled: true` from the frontmatter.
> 3. Move or rename if you want a different slug.
> 4. Optionally tighten the `description:` so it triggers correctly.

---

## Why this draft exists

`distill` saw the user ask for **{{TITLE}}** across {{FREQUENCY}} distinct sessions
in the scan window, with {{SIGNAL_WEIGHT}} explicit repetition signal(s).

Evidence quotes (from the scanner):

{{EVIDENCE_QUOTES_MARKDOWN}}

If those quotes don't actually share a common procedure, **delete this draft**
— the scanner over-grouped them.

---

## When to use

{{WHEN_TO_USE}}

## When NOT to use

{{WHEN_NOT_TO_USE}}

## Inputs

{{INPUTS_LIST}}

## Procedure

{{STEPS_NUMBERED}}

## Output

{{OUTPUT_SHAPE}}

---

## Notes for the human reviewer

{{NOTES_FROM_SCANNER}}

{{#EXTENDS}}
This draft extends an existing skill: **{{EXTENDS}}**. Consider whether it should
be merged into that skill as an additional workflow / sub-section rather than
becoming a new top-level skill.
{{/EXTENDS}}
